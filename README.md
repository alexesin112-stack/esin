# Практические задания по Эконометрике на компьютере (2 курс, 3 семестр)

# 1. LibreOffice (или Яндекс Таблицы). Случайные числа, гистограмма, описательная статистика (файл "03-2--Случайные числа и гистограмма.pdf").

Что сделано, рассчитано:

1.	Сгенерированы случайные числа по нормальному распределению с заданными параметрами:
— Среднее (μ) = 10,

— Среднее квадратическое отклонение (σ) = 4.

Всего сгенерировано 1000 значений (от №1 до №1000).

2.	Построена гистограмма распределения с помощью интервалов:

— Определена ширина интервала по формуле:

MAX−MINколичество интервалов−1количество интервалов−1MAX−MIN

— Заданы границы интервалов (с шагом от минимального значения).

— Рассчитана частота попадания значений в каждый интервал с помощью функции FREQUENCY.

3.	Применены описательные статистики:

— Использованы встроенные функции Excel для расчёта среднего и среднего квадратического отклонения.

— Эти параметры использованы для генерации данных через NORM.INV.

Вывод по работе:

В данной работе была смоделирована выборка из 1000 значений, распределённых по нормальному закону с заданными параметрами (μ=10, σ=4).

С помощью инструментов Excel были:

•	Сгенерированы случайные числа,

•	Построена гистограмма для наглядного представления распределения,

•	Проведена группировка данных по интервалам с расчётом частот.

Полученные результаты демонстрируют практическое применение методов генерации случайных данных и их визуализации в Excel.

Скриншот работы


# 2. LibreOffice (или Яндекс Таблицы). Парная корреляция [1, Лабораторная работа 1, с. 8 - 24].

Что сделано, рассчитано:

1.	Собраны и систематизированы парные данные по двум переменным x и y, всего 40 наблюдений.

2.	Рассчитаны вспомогательные показатели для анализа связи:

o	X^2 и y^2 (квадраты значений),

o	xy (произведение значений),

o	y^y^ (прогнозные значения по модели y^=3.5⋅x0.9y^=3.5⋅x0.9).

3.	Вычислены суммы и средние значения по всем столбцам, что необходимо для дальнейших статистических расчётов.

4.	Рассчитаны показатели отклонений для оценки модели:

o	(y^−y)^2 — квадраты ошибок модели,

o	(y−yˉ)^2 — квадраты отклонений фактических значений от среднего.

5.	Заданы критические значения для статистических тестов:

o	Tкрит для уровня значимости α=0.05 и числа степеней свободы df=20,

o	Fкрит для α=0.05 с df1=1 и df2=20.

Вывод по работе:

В данной работе проведён предварительный анализ парной зависимости между переменными x и y.
С использованием степенной регрессионной модели y^=3.5⋅x0.9 получены прогнозные значения, а также рассчитаны суммы квадратов отклонений, необходимые для оценки качества модели и проверки её статистической значимости.

Наличие критических значений t и F позволяет в дальнейшем провести:

•	t-тест для проверки значимости коэффициентов регрессии,

•	F-тест для оценки адекватности модели в целом.

Работа демонстрирует подготовительный этап корреляционно-регрессионного анализа.

Скринщот работы



# 3. LibreOffice (или Яндекс Таблицы). Парная регрессия [1, Лабораторная работа 2, с. 24 - 31].

Что сделано, рассчитано:

1.	Собраны и систематизированы парные данные по переменным x и y для 45 наблюдений.

2.	Построены два варианта регрессионных моделей:

o	Линейная регрессия после логарифмирования xx: модель вида

y^=−200+70⋅ln

o	Логарифмически преобразованные переменные: рассчитаны ln(x), ln(y), их квадраты и произведения.

3.	Рассчитаны вспомогательные показатели для анализа модели:

o	x2, y2, xy (для исходных данных),

o	ln2(x), ln2(y), ln(x)⋅ln(y) (для логарифмированных данных).

4.	Оценены ошибки модели:

o	Абсолютная относительная ошибка ∣(y^−y) : y∣,

o	Квадраты ошибок (y^−y)2,

o	Квадраты отклонений фактических значений от среднего (y−yˉ)2.

5.	Вычислены суммы и средние значения по всем столбцам для дальнейших статистических расчётов (например, для расчёта коэффициента детерминации R2, стандартной ошибки и тестов значимости).

Вывод по работе:

В работе проведён сравнительный анализ двух подходов к моделированию парной регрессии:

•	Модель с логарифмированием независимой переменной y^=−200+70⋅ln(x),

•	Модель с полностью логарифмированными переменными (подготовка для потенциальной степенной или линейной регрессии в логарифмах).

Рассчитанные ошибки и отклонения позволяют оценить качество подгонки модели и сравнить её с вариацией исходных данных. Наличие сумм квадратов ошибок и отклонений создаёт основу для расчёта коэффициента детерминации R2 и проведения тестов на адекватность модели.
Работа демонстрирует практический подход к выбору и оценке регрессионной модели при наличии нелинейной связи между переменными, а также важность преобразования данных для улучшения качества модели.

Скриншот работы

# 4. Практ задание по множественной регрессии [2, Раздел 2, с.45-89].

Ознакомиться с образцом и сделать свой вариант.

Подготовиться отвечать на контрольные вопросы.

Что сделано, рассчитано:

1.	Заданы исходные данные для трёх переменных:

— Зависимая переменная y,

— Независимые переменные x1 и x2,

— Их средние значения, средние квадратические отклонения и парные коэффициенты корреляции.

2.	Рассчитаны коэффициенты регрессионного уравнения вида:

y=a+b1x1+b2x2

где:

a≈0.247, b1≈0.779, b2≈0.840.

3.	Определены стандартизированные коэффициенты регрессии B1 и B2:

B1≈0.195, B2≈0.756, что показывает относительную силу влияния каждого фактора на y (сильнее влияет x2).

4.	Вычислены коэффициенты эластичности:

Э1≈0.279, Э2≈0.700, отражающие процентное изменение y при изменении соответствующего фактора на 1%.

5.	Проведён корреляционный анализ:

— Множественный коэффициент корреляции R≈0.858 показывает сильную связь y с обоими факторами,

— Коэффициент детерминации R2≈0.737 означает, что 73.7% вариации y объясняется моделью.

6.	Рассчитаны частные коэффициенты корреляции:

ry1/2≈0.324, ry2/1≈0.799, что показывает связь каждого фактора с y при фиксированном другом факторе.

7.	Проверка значимости модели с помощью F-критерия:

— Наблюдаемое значение Fнабл≈30.75,

— Критическое значение Fкрит≈0.051,

— Модель является статистически значимой (так как Fнабл>Fкрит)

Вывод по работе:

В работе была построена и проанализирована модель множественной линейной регрессии с двумя независимыми переменными.

Модель показала высокую объясняющую способность (R2=0.737) и статистическую значимость (подтверждена F-тестом).

Наибольшее влияние на зависимую переменную оказывает фактор x2 (стандартизированный коэффициент B2=0.756), что подтверждается также высоким частным коэффициентом корреляции ry2/1≈0.799.

Коэффициенты эластичности показывают, что изменение x1 на 1% приводит к изменению yy примерно на 0.28%, а изменение x2 — на 0.70%.

Работа демонстрирует полный цикл построения и оценки множественной регрессионной модели, включая расчёт параметров, оценку качества, проверку значимости и анализ относительной силы влияния факторов.

Скриншот работы

# 5. Практич.задание по нарушению классической модели множественной регрессии [2, Раздел 3, с.80-106].

Что сделано, рассчитано:

1.	Задана исходная модель регрессии и описательные статистики:

y=−0.6231+0.00004755⋅x1+0.01777⋅x2+e

— Средние значения xˉ1=3179.24, xˉ2=74.42, yˉ=0.85.

— Отклонения (видимо, стандартные): qx1=321.6, qx2=4.8, qy=0.1.

— Парные корреляции: rx1x2=0.7, ryx1=0.75, ryx2=0.96.

2.	Проверка на гетероскедастичность (непостоянство дисперсии ошибок):

— Тест Голдфельда–Квандта: F=6.0>5.05 → гетероскедастичность есть.

— Тест ранговой корреляции Спирмена: t=2.09>t0.025=2.069 → гетероскедастичность есть.

— Тест Уайта: W=8.75<χ5;0.052=11.07 → гетероскедастичности нет (возможно противоречие из-за малого n=25).

3.	Проверка на автокорреляцию остатков:

— Критерий Дарбина–Уотсона: DW=1.5 попадает в зону неопределенности (1.12<1.5<1.66).

— Q-статистика Льюинга–Бокса: Q=4.09<χ4;0.052=9.49 → автокорреляция отсутствует.

Вывод по работе:

В работе проведена диагностика нарушений классических предположений модели множественной линейной регрессии для уравнения с двумя независимыми переменными.

Основные результаты:

•	Гетероскедастичность, вероятно, присутствует (по тестам Голдфельда–Квандта и Спирмена), хотя тест Уайта её не подтверждает, что может быть связано с малой мощностью теста при небольшом объёме выборки (n=25).

•	Автокорреляция остатков, скорее всего, отсутствует (по критерию Льюинга–Бокса), однако критерий Дарбина–Уотсона даёт неоднозначный результат (зона неопределенности).

Таким образом, модель, возможно, страдает от гетероскедастичности, что может приводить к неэффективности оценок коэффициентов и некорректности статистических выводов. Для улучшения модели рекомендуется применение методов, устойчивых к гетероскедастичности (например, использование робастных стандартных ошибок), либо преобразование данных.

Работа демонстрирует важность диагностики регрессионных остатков и применение нескольких тестов для получения более надёжных выводов.

Скриншот работы

# 6. Нелинейная регрессия и способы реализации [2, Раздел 4, с.107-140].


Что сделано и рассчитано в работе:

1.	Построена линейная регрессионная модель на основе исходных данных (10 наблюдений).
Получено уравнение регрессии:

Y^=9.198+0.3454x

где:

o	a=9.198 — свободный член,

o	b=0.3454 — коэффициент наклона.

2.	Вычислены остатки модели ei=yi−y^i и проведена их проверка.

3.	Проверка на гетероскедастичность с помощью теста Голдфельда–Квандта:

o	Данные упорядочены по возрастанию x.

o	Исключены 4 центральных наблюдения, оставлены 3 в первой группе (малые x) и 3 во второй (большие x).

o	Рассчитаны суммы квадратов остатков:

RSS1=11.825, RSS2=41.877.

o	Получена статистика F=3F=3, которая оказалась меньше критического значения Fкрит(1,1)≈161 при α=0.05, гетероскедастичность не обнаружена.

4.	Проверка на гетероскедастичность с помощью теста Спирмена:

o	Проведено ранжирование по ∣ei∣ и xi.

o	Рассчитан коэффициент ранговой корреляции Спирмена rs.

o	Проверена его значимость с помощью t-критерия:

t=2.487>tкрит(0.025;8)=2.306, что указывает на наличие гетероскедастичности.

5.	Упомянут тест Уайта как дополнительный метод проверки гетероскедастичности, но расчёты не приведены.

Вывод по работе:

В ходе работы была построена и проанализирована линейная регрессионная модель. Проведены два теста на гетероскедастичность:

•	Тест Голдфельда–Квандта не выявил гетероскедастичности.

•	Тест Спирмена, напротив, показал её наличие, что может свидетельствовать о непостоянстве дисперсии ошибок в зависимости от значений фактора x.

Рекомендации:

В случае подтверждения гетероскедастичности (например, по тесту Спирмена) следует рассмотреть возможность:

•	применения обобщённого метода наименьших квадратов (ОМНК),

•	использования робастных стандартных ошибок (например, по Уайту),

•	преобразования переменных для стабилизации дисперсии.

Итог: модель построена, но требует уточнения в части проверки предпосылок классической регрессии, особенно в части гомоскедастичности остатков.

Скриншоты работы

# 7. Анализ данных в R [3, общее задание и задание по вариантам]. Отчет по анализу данных mtcars.

1) Что сделано / рассчитано

- Загружен набор данных mtcars (32 наблюдения, 11 переменных).

- Выведены структура данных (str), краткая сводка (summary) и первые 6 строк (head).

- Посчитаны описательные статистики для переменной mpg (мили на галлон):

 - Среднее (mean) = 20.09

 - Медиана = 19.20

 - Стандартное отклонение (sd) ≈ 6.03

 - Минимум = 10.40, максимум = 33.90

 - 1-й квартиль (Q1) = 15.425, 3-й квартиль (Q3) = 22.80

- Добавлена новая переменная kpl = mpg × 0.425 (км/л) и вычислено её среднее:

 - mean(kpl) ≈ 8.54 км/л

- Построена гистограмма мощности двигателя (hp) с отметкой среднего:

 - Выявлена правая скошенность распределения; большинство автомобилей в диапазоне ≈50–150 л.с.; есть выбросы >200 л.с.

- Построена диаграмма рассеяния mpg vs wt; рассчитана линейная регрессия и коэффициент корреляции:

 - Модель lm(mpg ~ wt): intercept ≈ 37.285, slope ≈ −5.345 (линия регрессии на графике)
 - Коэффициент корреляции r ≈ −0.868

2) Вывод

- Набор mtcars небольшой (32 автомобиля) и содержит характеристики двигателей и автомобилей.
- Для mpg наблюдается умеренный разброс (sd ≈ 6), при этом среднее немного выше медианы, что указывает на небольшую правую асимметрию распределения mpg.

- Конвертация в км/л даёт среднюю топливную эффективность ≈ 8.54 км/л.

- Мощность двигателей (hp) распределена несимметрично с правым хвостом — есть несколько мощных автомобилей, влияющих на среднее.

- Сильная отрицательная линейная связь между весом автомобиля и расходом топлива: чем тяжелее автомобиль, тем меньше mpg (r ≈ −0.868). Линейная модель показывает, что при увеличении wt на 1 (тыс. фунтов) ожидаемое снижение mpg примерно на 5.35 миль/галлон.

- Практическое следствие: вес автомобиля — важный фактор топливной эффективности; для прогнозирования mpg вес даёт сильный и интерпретируемый вклад.

Скринощот работы

